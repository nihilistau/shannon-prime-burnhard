build/
*.exe
*.obj
*.o
*.a

build*

# Local-only LM Studio credentials used by shannon-prime-lmstudio-server.
# Never committed.
lmstudio-sp-token.txt              # REST bearer (sk-lm-...)
lmstudio-sp-sdk-passkey.txt        # SDK WS pair (clientIdentifier:passkey)
